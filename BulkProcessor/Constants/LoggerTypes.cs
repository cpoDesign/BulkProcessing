namespace BulkProcessor.Constants
{
    /// <summary>
    /// Logger types Each logger type is represented with different color.
    /// </summary>
    public enum LoggerTypes
    {
        Trace,
        Log,
        System
    }
}